# ML dependencies for disaster prediction
# Run: pip install -r requirements-ml.txt

# ============================================================
# Inference / prediction API (runtime — always installed)
# ============================================================
# The trained model is a PyTorch multi-task network (models/disaster_nn.pt).
# Inference needs only torch + numpy: the StandardScaler mean/scale are baked
# into the checkpoint, so scikit-learn is NOT required to serve predictions.
#
# CPU wheels (default). For CUDA, install the matching build from pytorch.org.
torch>=2.2.0
numpy>=1.26.0

# ============================================================
# Training only (model/train_pytorch.py) — not needed to run predictions
# ============================================================
# scikit-learn provides StandardScaler + metrics during training; pandas builds
# the training feature frame. Install these only on the box that (re)trains:
#
#   pip install scikit-learn>=1.4.0 pandas>=2.0.0

# ============================================================
# GPU acceleration (optional)
# ============================================================
# Install the CUDA build of torch from https://pytorch.org for GPU inference.
